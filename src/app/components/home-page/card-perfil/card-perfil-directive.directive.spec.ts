/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CardPerfilDirectiveDirective } from './card-perfil-directive.directive';

describe('Directive: CardPerfilDirective', () => {
  it('should create an instance', () => {
    const directive = new CardPerfilDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
